UPDATE xxpa_proj_milestone_manage xpmm
   SET xpmm.ba_fully_packing_date      = NULL,
       xpmm.fully_delivery_date        = NULL,
       
       xpmm.last_updated_by            = fnd_global.user_id,
       xpmm.last_update_date           = SYSDATE,
       xpmm.program_update_date        = SYSDATE

 WHERE task_id = /*p_proj_milestone.task_id*/7482238
  /* AND (nvl(frozen_flag, 'N') = 'N' OR
       nvl(frozen_flag, 'N') = 'Y' AND
       to_date(p_proj_milestone.period_name, 'MON-YY') >=
       to_date(fnd_profile.value('XXPA_MILESTONE_START_DATE'),
                'DD-MON-YYYY'))*/;
/*BEGIN
  fnd_global.APPS_INITIALIZE(user_id      => 4270,
                             resp_id      => 50676,
                             resp_appl_id => 660);
  mo_global.init('M');
END;*/
CREATE TABLE xxpa_proj_ms_manage180727 AS
SELECT * FROM xxpa_proj_milestone_manage xpmm WHERE xpmm.task_id = 7482238;

SELECT * FROM xxpa_proj_ms_manage180727;

SELECT * FROM xxpa_proj_milestone_manage WHERE task_id = /*p_proj_milestone.task_id*/7482238;
